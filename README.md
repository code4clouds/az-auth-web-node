# az-auth-web-node
A NodeJS service with a web frontend that authenticates with Azure.

# Setup
``` bash
npm install
```

# Run
``` bash
npm  start
```

# Container
`` bash
docker build -t az-auth-web-node .
``